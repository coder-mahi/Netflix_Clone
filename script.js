let signbtn = getElementByClassName('sign-in-btn');
function signin(){
    let email = getElementById('mail-input');
    let pwd = getElementById('pwd-input');
    if(email.text =="" || pwd.text ==""){
        window.alert("");
    }
}